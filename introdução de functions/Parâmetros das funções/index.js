//parâmetros
function soma(velocity, aceleração) {
    NovaSoma = velocity + aceleração
    console.log(`Velocidade de ${NovaSoma}Km/h`)
}
//vamos chamar a função e atribuir os valores
soma(8, 5)
soma(10, 65)
soma(10, 14)