
// neste arquivo vamos colocar a classe

class Property {
       constructor(kind, area, rented) {
       this.kind = kind // propriedade tipo
       this.area = area // propriedade area
       this.rented = rented // propriedade alugada
    }
}
    